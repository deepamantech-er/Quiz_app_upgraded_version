Quiz App is a command-line quiz management application built with Python. It allows users to add questions with multiple-choice options, delete existing quizzes, and attempt quizzes while keeping track of their scores. The application uses JSON for data persistence, allowing quiz data to be stored and loaded across different program sessions. It also demonstrates concepts such as functions, loops, conditional statements, exception handling, input validation, dictionaries, lists, and file handling.